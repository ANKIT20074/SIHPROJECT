{
  "name": "doctorkavach",
  "version": "2.0.0",
  "description": "DoctorKavach — Healthcare Verification & OPD Queue Management Platform for India (SIH Prototype)",
  "main": "api/index.js",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "vercel-build": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "node server/src/index.js",
    "server": "node server/src/index.js",
    "dev": "npm --prefix frontend run dev"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1"
  }
}
